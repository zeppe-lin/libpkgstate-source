# 3.0 extraction

Before 3.0, `libpkgstate-source` was an optional product in the `libpkgstate` repository and shared its release version with the durable state owner. It is now an independent role-qualified repository with its existing SONAME generation `1` preserved.

No implementation body was synthesized during extraction. Exact origin hashes are recorded in `libpkgstate-2.5.1-origin.sha256`.
